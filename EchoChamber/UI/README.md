# EchoChamber WebView UI (React 19 + Vite)

Copied from `plugforge/juce-templates/WebViewUI`.

```bash
cd UI
npm install
npm run dev      # browser preview
npm run build    # emits to ../ui for plugin BinaryData
```

- Personality: **modern** (#00FFAA)
- Bridge: window.__woManusSetParam / window.__woManusParamChanged
- Max 7 knobs visible; Expand reveals advanced
- Spectrum: false · GR meter: false
